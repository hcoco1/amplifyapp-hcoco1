type Note @model @auth(rules: [{allow: owner}, {allow: private}]) {
  id: ID!
 
  period: String
  username: String
  afe: String
  process: String
  error: String
  coaching: String
  durable: String

}
 