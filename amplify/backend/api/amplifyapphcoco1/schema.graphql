type Todo @model @auth(rules: [{allow: public}, {allow: owner}]) {
  id: ID!
  auditor: String
  period: String
  username: String
  afe: String
  process: String
  error: String
  coaching: String
  durable: String
}
 